STRING:
    ('\'' @ (('\\' . ) | [^'] )* '\'')

    {@}
;
NUMBER:

    @ [+-]? @ ( [0-9]+ ) @ ([.,] [0-9]+)?
    
    @{sign, main, dec}
;
BOOLEAN:
    &d('true' | 'false')

    {d}
;
array: 

    '[' ( ( @ expr ',')* )? ']'

    {@}

object:
    '{'
        ( @ ID | NUMBER ':' @ expr ( ',' @ ID | NUMBER ':' @ expr )* ';' )?
    '}'
    @{key, value, keys, values}
any_data: 

    @ BOOLEAN | NUMBER | STRING | ID | AT | array | object
    
    {@}
;
