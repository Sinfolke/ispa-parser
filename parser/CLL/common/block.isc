// TODO: possibly to merge into a single block rule
cll_block:
    '{' Rule.rule* '}'

    data: $1;
;
cll_spaced_block(spaces_amount):
    $while (skipup(' ') > spaces_amount)
        Rule#rule
    data: %1;
;
