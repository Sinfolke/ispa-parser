/*
    CLL (COMMON LOGIC LANGUAGE) is the programming language.
    Might need it when you have to create a complex rule or to process data before render output.  
    see cll.doc for documentation
*/

import common/*;
import variable/*;
import function/*;

cll:
    // copiable_method_call does not require braces here
    &cll_begin('\n' '$') (var | cond | expr | copiable_method_call)+ 

    data: $2;
;
